ชื่อ: เหมราช กิ่งพลอยเดิม

รหัสนักศึกษา: 683120100121

# OOP Class Diagram Assignment

## 📌 รายละเอียดงาน
โปรแกรมนี้พัฒนาตาม Class Diagram ที่กำหนด โดยใช้หลักการเขียนโปรแกรมเชิงวัตถุ (Object-Oriented Programming)

## 📦 โครงสร้างโปรเจกต์

---

> งานนี้เป็นการเขียนโปรแกรมตาม Class Diagram โดยใช้ภาษา C# และหลักการ OOP  
> มีการใช้ Encapsulation ผ่าน Properties มี method แสดงผล และสร้าง instance เพื่อเรียกใช้งาน  
> พร้อมทั้งอัปโหลด source code และ class diagram ขึ้น GitHub เรียบร้อยแล้ว

---

> โปรแกรมนี้พัฒนาตาม Class Diagram ที่กำหนด โดยใช้หลักการเขียนโปรแกรมเชิงวัตถุ  
> มีการกำหนด attribute เป็น private และเข้าถึงผ่าน Properties ตามหลัก Encapsulation  
> แต่ละคลาสมี constructor, method area(), volume() และ printInfo()  
> พร้อมทั้งสร้าง instance เพื่อทดสอบการทำงาน และ push source code พร้อม class diagram ขึ้น GitHub ภายในกำหนด

---


## 🧩 Class Diagram
โปรแกรมประกอบด้วย 3 คลาส
- Circle
- Rectangle
- Cylinder

แต่ละคลาสมี attribute เป็น private และเข้าถึงผ่าน Properties  
มี method `area()`, `volume()` และ `printInfo()` ตามที่กำหนดใน diagram

## 🛠️ หลักการที่ใช้
- Encapsulation ด้วย Properties
- Constructor ตาม <<create>>
- Method แสดงผล (printInfo)
- การสร้าง instance เพื่อเรียกใช้งาน

## ▶️ วิธีรันโปรแกรม (C#)
```bash
dotnet run


# 3️⃣ โค้ดภาษา C# (แยกไฟล์)

## 📄 Circle.cs
```csharp
using System;

class Circle
{
    private double radius;

    public double Radius
    {
        get { return radius; }
        private set { radius = value; }
    }

    public Circle(double radius)
    {
        Radius = radius;
    }

    public double area()
    {
        return Math.PI * radius * radius;
    }

    public void printInfo()
    {
        Console.WriteLine("Circle");
        Console.WriteLine("Radius = " + radius);
        Console.WriteLine("Area = " + area());
    }
}
